export const environment = {
  production: false,
  GEMINI_API_KEY: 'REPLACE_ME_GEMINI_API_KEY',
  API_URL: '' // url for your hosted backend (e.g., https://my-backend.com). If empty, it uses relative path for local development
};
